<UserControl x:Class="RenderEngineDesktop.Views.RenderEngineFields"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:RenderEngineDesktop.Controls"
             xmlns:process="clr-namespace:RenderEngineDesktop.Models.Process"
             xmlns:fields="clr-namespace:RenderEngineDesktop.Converters.Fields"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="500">
    <UserControl.DataContext>
        <process:GenericProcessModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <fields:SourceConverter x:Key="SourceConverter"/>
        <fields:TemplateConverter x:Key="TemplateConverter"/>
        <fields:OutputFileConverter x:Key="OutputFileConverter"/>
        <fields:OutputFilesConverter x:Key="OutputFilesConverter"/>
        <fields:MaximumInPixelsConverter x:Key="MaximumInPixelsConverter"/>
        <fields:OutlineColorComponentConverter x:Key="OutlineColorComponentConverter"/>
        <fields:CultureConverter x:Key="CultureConverter"/>
        <fields:LeadsConverter x:Key="LeadsConverter"/>
        <fields:PercentTranslationConverter x:Key="PercentTranslationConverter"/>
        <fields:PercentageConverter x:Key="PercentageConverter"/>
        <fields:WatermarkConverter x:Key="WatermarkConverter"/>
        <fields:ZipFileConverter x:Key="ZipFileConverter"/>

        <Style TargetType="StackPanel">
            <Setter Property="Orientation" Value="Vertical"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <controls:SourceFields x:Name="SourceFields" Context="{Binding Converter={StaticResource SourceConverter}}"/>
        <controls:TemplateFields x:Name="TemplateFields" Context="{Binding Converter={StaticResource TemplateConverter}}"/>
        <controls:PathFields x:Name="OutputFileFields" Context="{Binding Converter={StaticResource OutputFileConverter}}"  Label="OutputFilePath"/>
        <controls:PathsFields x:Name="OutputFilesFields" Context="{Binding Converter={StaticResource OutputFilesConverter}}"/>
        <controls:PathFields x:Name ="ZipFileFields" Context="{Binding Converter={StaticResource ZipFileConverter}}" Label="ZipFile"/>
        <controls:SizeFields x:Name="MaximumInPixelsFields" Context="{Binding  Converter={StaticResource MaximumInPixelsConverter}}" Label="Maximum (in pixels)"/>
        <controls:OutlineColorComponentFields x:Name="OutlineColorComponentFields" Context="{Binding Converter={StaticResource OutlineColorComponentConverter}}"/>
        <controls:WatermarkFields x:Name="WatermarkFields" Context="{Binding  Converter={StaticResource WatermarkConverter}}"/>
        <controls:CultureFields x:Name="CultureFields" Context="{Binding Converter={StaticResource CultureConverter}}"/>
        <controls:PointFields x:Name="PercentTranslationFields" Context="{Binding Converter={StaticResource PercentTranslationConverter}}" Label="Percent Translation"/>
        <controls:SizeFields x:Name="PercentageFields" Context="{Binding Converter={StaticResource PercentageConverter}}" Label="Percentage"/>
    </StackPanel>

</UserControl>
