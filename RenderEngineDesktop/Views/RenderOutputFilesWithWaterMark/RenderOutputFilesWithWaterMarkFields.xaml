<UserControl x:Class="RenderEngineDesktop.Views.RenderOutputFilesWithWaterMark.RenderOutputFilesWithWaterMarkFields"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:RenderEngineDesktop.Models"
             xmlns:controls="clr-namespace:RenderEngineDesktop.Controls"
             xmlns:process="clr-namespace:RenderEngineDesktop.Models.Process"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <process:RenderOutputFilesWithWaterMarkModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="StackPanel">
            <Setter Property="Orientation" Value="Vertical"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <controls:SourceFields Context="{Binding Source}"/>
        <controls:TemplateFields Context="{Binding Template}"/>
        <controls:PathsFields Context="{Binding OutputFiles}"/>
        <controls:WatermarkFields Context="{Binding WaterMark}"/>
    </StackPanel>
</UserControl>
