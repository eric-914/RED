<UserControl x:Class="RenderEngineDesktop.Views.RenderPreview.RenderPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:renderPreview="clr-namespace:RenderEngineDesktop.Views.RenderPreview"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <renderPreview:RenderPreviewViewModel/>
    </UserControl.DataContext>

    <DockPanel>
        <StackPanel DockPanel.Dock="Left" Name="RpvFieldsStackPanel">
            <renderPreview:RenderPreviewFields DataContext="{Binding}"/>
            <Button Command="{Binding InvokeCommand}" Margin="10" IsDefault="True">Invoke</Button>
        </StackPanel>
        <StackPanel Background="Gray" Name="RpvImageStackPanel">
            <Border BorderBrush="Black" BorderThickness="2">
                <Image Source="{Binding Image}"/>
            </Border>
        </StackPanel>
    </DockPanel>
</UserControl>
